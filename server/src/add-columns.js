const { Pool } = require('pg'); require('dotenv').config({ path: '../.env' }); const pool = new Pool({ connectionString: process.env.DATABASE_URL }); async function run() { try { await pool.query('ALTER TABLE interviewers ADD COLUMN IF NOT EXISTS email_notifications BOOLEAN DEFAULT TRUE;'); await pool.query('ALTER TABLE interviewers ADD COLUMN IF NOT EXISTS whatsapp_notifications BOOLEAN DEFAULT TRUE;'); console.log('Columns added'); } catch(e) { console.error(e); } finally { pool.end(); } } run();
